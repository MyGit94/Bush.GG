<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper	namespace="com.pinkward.bushgg.domain.member.mapper.MemberMapper">

	<select id="findByAll" resultType="MemberDTO">
		SELECT loginid, nickname, passwd, email, TO_CHAR(regdate, 'yyyy-MM-DD') regdate, role
		FROM member
		ORDER BY regdate DESC
	</select> 
	
	<select id="findByNick" parameterType="string" resultType="MemberDTO">
		SELECT loginId, nickName, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate, role
		FROM member
		WHERE id = #{value}
	</select>
	
	<select id="findByIdAndPasswd" resultType="Member">
		SELECT id, name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate
		FROM member
		WHERE id = #{id} AND passwd = #{passwd}
	</select>
	
	<select id="findByNameLike" parameterType="string"
		resultType="Member">
		SELECT id, name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate
		FROM member
		WHERE name LIKE '%' || #{value} || '%'
	</select>  
	
	<insert id="create" parameterType="MemberDTO">
		INSERT INTO member(		loginId,
							    nickName,
							    passwd,
							    email,
						   		role)
		VALUES(#{loginId}, #{nickName}, #{passwd}, #{email}, #{role})
	</insert>
<!--  
	<update id="update" parameterType="Member">
		UPDATE member
		SET passwd = #{passwd}, email = #{email}
		WHERE id = #{id}
	</update> -->
	
	<!-- 동적 SQL 활용 -->
	<!--
	<select id="findBySearchType" parameterType="string"
		resultType="Member">
		SELECT id, name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate
		FROM member
		<where>
			<if test="type != null and type == 'id'">
				and id = #{value}
			</if>
			<if test="type != null and type == 'name'">
				and name LIKE '%' || #{value} || '%'
			</if>
		</where>
	</select>
	
	<select id="findBySearchAll" parameterType="string"	resultType="Member">
		SELECT id, name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate
		FROM member
		WHERE id = #{value} OR name LIKE '%' || #{value} || '%'
	</select>
	
	<select id="findBySearchAllOption" parameterType="MemberSearchCondition" resultType="Member">
		SELECT id, name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate
		FROM member
		<where>
			<if test="memberId != null and memberId != ''">
				or id = #{memberId}
			</if>
			<if test="name != null and name != ''">
				or name LIKE '%' || #{name} || '%'
			</if>
			<if test="email != null and email != ''">
				or email = #{email}
			</if>
		</where>
	</select>  -->
	
</mapper>